What do the O and L options in Listing 10 mean?

curl -h | less
/-O
#-O --remote-name Write output to a file named as the remote file

/-L
# L, --location      Follow redirects (H)
#     --location-trusted  Like '--location', and send auth to
